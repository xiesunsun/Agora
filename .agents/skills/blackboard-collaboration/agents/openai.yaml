interface:
  display_name: "Blackboard Collaboration"
  short_description: "Open and run a live Blackboard session"
  default_prompt: "Use $blackboard-collaboration to create a live Blackboard session, return the session link, and collaborate through the blackboard page."

policy:
  allow_implicit_invocation: true
